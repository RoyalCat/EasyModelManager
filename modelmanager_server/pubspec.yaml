name: modelmanager_server
description: An empty Aqueduct application.
version: 0.0.1
author: stable|kernel <jobs@stablekernel.com>

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  uuid: ^2.0.4
  mime: ^0.9.6+3
  http_server: ^0.9.8+3
  uri: 0.11.3+1
  shared_models:
    path: ../shared_models

dev_dependencies:
  test: ^1.0.0